name: System Tests (E2E)

on:
  pull_request:
    branches: [main]

jobs:
  e2e-skipped:
    runs-on: ubuntu-latest
    steps:
      - name: No E2E config
        run: echo "E2E tests are not configured for this project — skipping"
